# 特效资产

循环特效（雨/雪/速度线/星闪）与单帧特效（冲击/闪光）**参数集**，无独立样张（样张 = 渲染帧缩略）。

- **格式**：`fx.json`（参数化，接 skill `scripts/fx.py`）
- **消费端**：`fx.py` 特效函数加载参数
- **检索**：`asset_cli.py search -c 特效 -k 雪`
- 首批试点：雪花（demo_fx 参数）