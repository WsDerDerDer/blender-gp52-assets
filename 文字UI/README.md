# 文字/UI 资产

字幕 / 标题 / 气泡 / 打字机样式（非艺术字用 FONT 直渲，艺术字走 gp_text）。

- **格式**：`text_style.json`（对齐/阴影/字距/气泡边距参数 + 字体约定）
- **消费端**：`text_native` / `gp_text`
- **检索**：`asset_cli.py search -c 文字UI -k 气泡`
- 首批试点：对话气泡样式