# 动物资产

四足 / 飞行 / 水生动物角色（Q 版胶囊木偶模板变体 + 配件）。

- **格式**：`char_spec.json`（rig 模板名记在 `rig_type` 字段），复数动物模型见 `rig.quadruped_q`
- **消费端**：`cast.Character(spec)` / 现有 demo 复用
- **检索**：`asset_cli.py search -c 动物 -k <名称>`
- 首批试点：Q 版猫（demo_animal 的 trot 猫）